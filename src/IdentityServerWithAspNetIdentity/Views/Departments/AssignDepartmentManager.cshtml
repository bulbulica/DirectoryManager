@model AssignDepartmentManager
@{
    Layout = "/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Assign Department Manager to " + Model.Department.Name;
}

<h3>Add Students to activity: @Model.Department.Name</h3>

<div class="row">
    <div class="col-md-4">
        <form asp-action="AssignDepartmentManager"  method="post">
            <div asp-validation-summary="All" class="text-danger hidden"></div>
            <input type="hidden" asp-for="Department.Id" value="@Model.Department.Id" class="form-control" />
            <div class="form-group">
                <label asp-for="IdDepartmentManager" class="control-label">Select new DepartmentManager</label>
                <br /><br />
                <select asp-for="IdDepartmentManager" id="mainselection">
                @{
                    if (Model.Employees.ToList().Count != 0)
                    {
                        var employeesList = Model.Employees.ToList();
                        foreach (var employee in employeesList)
                        {
                            <option value="@employee.Id">@employee.Name</option>
                        }
                    }
                }
                </select>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
            </div>
        </form>
        </div>
</div>
<div>
    <br />
    <a asp-action="ManageDepartments">Back to Departments</a>
</div>
